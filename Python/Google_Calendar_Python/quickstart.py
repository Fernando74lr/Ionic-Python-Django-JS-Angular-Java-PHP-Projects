from res.functions import createEvent, deleteEvent, readEvent, updateEvent
from res.connection import service

def main():
    # CREATE
    # createEvent(service);
    # READ
    # readEvent(service)
    # UPDATE
    # updateEvent(service)
    # DELETE
    # deleteEvent(service)

if __name__ == '__main__':
    main()